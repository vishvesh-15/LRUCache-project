# LRUCache

- This is Implemented at DataStructure level
- This is created using Linked List and with Hashmap
- Which supports the following operations in O(1) complexity
    - insertKeyValue()
    - getValue()
    - getMostRecentkey()
 
